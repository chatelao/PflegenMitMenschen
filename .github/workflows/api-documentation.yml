name: API Documentation

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install Redocly CLI
        run: npm ci
      - name: Validate OpenAPI definition
        run: npx redocly lint api/openapi.yaml

  publish:
    needs: validate
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - name: Install Redocly CLI
        run: npm ci
      - name: Build Redoc HTML
        run: npx redocly build-docs api/openapi.yaml --output=public/index.html
      - name: Copy Web App
        run: |
          mkdir -p public/web
          cp -r web/app/* public/web/
      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: 'true'
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'public'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
